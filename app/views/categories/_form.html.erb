<%= form_with(model: @category, local: true) do |form| %>
  <div class="field">
    <p><%= form.label :name %></p>
    <p><%= form.text_field :name %></p>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>

<% end %>